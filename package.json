{
  "name": "elm-programming-ladder",
  "description": "Programming ladder",
  "version": "0.8.6",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/knowit/elm-programming-ladder"
  },
  "scripts": {
    "start": "webpack-dev-server --hot --inline",
    "prebuild": "rimraf dist",
    "build": "webpack",
    "reinstall": "npm i rimraf && rimraf node_modules && npm uninstall -g elm && npm i -g elm && npm i && elm package install"
  },
  "devDependencies": {
    "autoprefixer": "^6.7.7",
    "elm": "^0.18.0",
    "node-sass": "^4.5.1",
    "rimraf": "^2.6.1",
  },
  "dependencies": {
  }
}
